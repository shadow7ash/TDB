{
  "name": "Terabox Downloader Bot",
  "description": "A Telegram bot to download files from TeraBox links.",
  "repository": "https://github.com/yourusername/yourrepository",
  "env": {
    "TELEGRAM_BOT_TOKEN": {
      "description": "The token for your Telegram bot",
      "required": true
    },
    "MONGODB_URI": {
      "description": "The URI for your MongoDB database",
      "required": true
    },
    "RAPIDAPI_KEY": {
      "description": "Your RapidAPI key for accessing the TeraBox API",
      "required": true
    }
  },
  "formation": {
    "worker": {
      "quantity": 1,
      "size": "free"
    }
  },
  "addons": [
    "mongolab"
  ]
}
